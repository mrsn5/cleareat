import { OnlyForAdminDirective } from './only-for-admin.directive';

describe('OnlyForAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyForAdminDirective();
    expect(directive).toBeTruthy();
  });
});
