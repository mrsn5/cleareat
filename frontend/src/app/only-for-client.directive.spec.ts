import { OnlyForClientDirective } from './only-for-client.directive';

describe('OnlyForClientDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyForClientDirective();
    expect(directive).toBeTruthy();
  });
});
